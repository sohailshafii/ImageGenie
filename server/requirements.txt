# Backend (server) runtime deps — installed into the worker/API container image.
# SQLAlchemy + psycopg for the Postgres metadata DB; pydantic-settings for
# env-var config; google-cloud-pubsub for the queue; objaverse for the download
# worker's mesh fetch + uid enumeration.
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4
pydantic-settings==2.14.2
google-cloud-pubsub==2.39.0
google-cloud-storage==2.19.0
objaverse==0.1.7
# Cloud Run push receiver (server/app/web.py) + backend-for-frontend API
# (server/app/api.py); bcrypt hashes account passwords (server/app/security.py).
fastapi==0.139.2
uvicorn[standard]==0.51.0
bcrypt==4.2.1
# Transactional email: Resend is one POST, so httpx rather than its SDK (app/mail.py).
httpx==0.28.1
# Preprocessing stages (convert/normalize/render) — mesh IO + offscreen render.
# numpy pinned <2 for pyrender 0.1.x compat; pyrender renders headless via OSMesa
# (system libs installed in the Dockerfile). trimesh loads GLB + exports PLY.
numpy==1.26.4
trimesh==4.4.9
pyrender==0.1.45
Pillow==10.4.0
# pyrender's OSMesa backend imports OSMesaCreateContextAttribs, which upstream
# PyOpenGL (incl. 3.1.0, the version pyrender hard-pins) never exposed. The pyrender
# author's fork adds it and self-reports as 3.1.0, satisfying that pin. Installed
# from git (needs `git` at build time — see the Dockerfile) for headless rendering.
PyOpenGL @ git+https://github.com/mmatl/pyopengl.git
